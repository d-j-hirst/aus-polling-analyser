#include "QuickTest.h"

#include "General.h"
#include "PollingProject.h"

#include "Log.h"
#include "RandomGenerator.h"

QuickTest::QuickTest()
{
	//PollingProject project = PollingProject("Federal election 2019.pol");
	//project.models().run(0);
	//project.projections().run(0);
	//project.simulations().run(0);
	//project.simulations().run(0);
	//wxMessageBox(formatFloat(project.simulations().view(0).getLatestReport().getPartyOverallWinPercent(Simulation::MajorParty::One), 2));

	//RandomGenerator rng;
	//for (int a = 0; a < 100; ++a) {
	//	PA_LOG_VAR(rng.beta(2.0, 10.0));
	//}
}
